# Umeed-e-Sahar — GitHub-ready website

Upload the contents of this folder to the ROOT of the GitHub repository. Do not upload the ZIP inside the repository.

For Netlify: connect this repository as the site's continuous deployment source. This is a plain HTML/CSS/JS site, so no build command is required and the publish directory is the repository root.
